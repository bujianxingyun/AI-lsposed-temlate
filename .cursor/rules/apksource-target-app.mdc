---
description: apksource 目标应用源码/反编译代码的分析与 Hook 参考规范
globs: apksource/**
alwaysApply: false
---

# apksource 使用规范

`apksource/` 存放 **Hook 目标应用** 的源码或反编译结果，**只读参考**，不加入 `module/` 编译。

## 目录约定

- 每个目标 app 一个子目录，命名为 **包名**，如 `apksource/com.example.target/`
- 可选 `NOTES.md` 记录版本、反编译工具、混淆情况

## AI 分析时应

1. 在此目录查找类名、方法签名、调用关系，再编写 `module/.../MainModule.java` 中的 Hook
2. Hook 使用目标进程 `ClassLoader` 加载类，**禁止**把 apksource 中的类复制进模块工程
3. 注意混淆：jadx 中的 `a.b.c` 可能与运行时不同，需结合 mapping 或运行时验证
4. 匹配 overload 时列出完整参数类型（`int.class` vs `Integer.class`）

## 与模块配置对齐

| 模块 | apksource |
|------|-----------|
| `TARGET_PACKAGE` / `scope.list` | 子目录包名 |
| `onPackageReady` 过滤 | 只 Hook scope 内应用 |

## 输出 Hook 代码时

- 类名、方法名来自 apksource 分析结果
- 代码写入 `module/app/src/main/java/`，遵循 `lsposed-hooking` 规则（API 101、`onPackageReady`）
