---
description: 公众网骨架屏加载规范。涉及列表页、房源列表、浏览记录等首屏数据加载时遵循。
globs: src/pages/**/*.vue,src/components/**/*.vue
alwaysApply: false
---

# 骨架屏加载

参考：`design/公众网全局提示规范.md` 第四章

## 适用场景

打开新页面后再请求数据的列表页，例如：

- 我要买房 / 我要租房 房源列表
- 合同列表、浏览记录、关注列表
- 经纪机构/人员公示列表

**不适用**：表单提交、按钮触发的单次查询（用加载遮罩，见 toast-loading 规范）。

## 实现流程

```javascript
data() {
  return { loading: true, list: [] }
},
async mounted() {
  this.loading = true
  try {
    this.list = await fetchList()
  } finally {
    this.loading = false
  }
}
```

```vue
<template>
  <div class="house-list">
    <template v-if="loading">
      <SkeletonHouseCard v-for="n in 4" :key="n" />
    </template>
    <template v-else>
      <HouseCard v-for="item in list" :key="item.id" :data="item" />
    </template>
  </div>
</template>
```

首屏显示 **3–5 条**骨架项，数据返回后整体替换为真实内容。

## 房源卡片骨架屏结构

```
.skeleton-house-card
├── .skeleton-house-image.skeleton      (240×180)
└── .skeleton-house-info
    ├── .skeleton-house-title.skeleton  (高 20px, 宽 70%)
    ├── .skeleton-house-community       (高 16px, 宽 40%)
    └── .skeleton-house-details         (高 14px, 宽 60%)
```

## 通用列表骨架屏

合同、浏览记录等使用 `.skeleton-list-item`：

- 左侧图片区：120×90
- 右侧内容区：title(70%) + meta(50%) + time(40%)

## 动画样式

```scss
@keyframes skeleton-loading {
  0%   { background-position: -200px 0; }
  100% { background-position: calc(200px + 100%) 0; }
}

.skeleton {
  background: linear-gradient(90deg, #f0f0f0 25%, #e0e0e0 50%, #f0f0f0 75%);
  background-size: 200px 100%;
  animation: skeleton-loading 1.5s ease-in-out infinite;
  border-radius: 4px;
}
```

## 禁止事项

- 列表首屏加载使用 `v-loading` 全屏遮罩代替骨架屏
- 骨架屏动画过于复杂或条目过多（>5）
- 骨架布局与真实卡片结构不一致导致 CLS 跳动过大
