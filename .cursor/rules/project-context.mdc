---
description: LSPosed 模块模板项目结构与定制流程
alwaysApply: true
---

# 项目上下文

本仓库是 **LSPosed / libxposed API 100** 的 Android Xposed 模块模板，Gradle 工程位于 `module/`。

## 目录结构

| 路径 | 用途 |
|------|------|
| `module/app/src/main/java/` | 模块入口与 Hook 逻辑（`MainModule extends XposedModule`） |
| `module/app/src/main/resources/META-INF/xposed/` | `java_init.list`、`scope.list`、`module.prop` |
| `module/app/proguard-rules.pro` | Release 混淆规则，必须保留入口与 Hooker |
| `module/libxposed-compat/` | `@XposedHooker` 注解兼容层 |
| `.cursor/skills/xposed-framework-api/` | 详细 API 参考，编写 Hook 时优先查阅 |

## 定制新模块时的同步修改

修改包名时，以下文件必须保持一致：

1. `module/app/build.gradle` — `namespace`、`applicationId`
2. Java 包目录与 `MainModule.java` 的 `package` 声明
3. `META-INF/xposed/java_init.list` — 入口类 FQCN
4. `AndroidManifest.xml` 中的 `android:label` / `android:description`

## 通用原则

- libxposed API 使用 `compileOnly`，**禁止**打进 APK
- 始终用 `param.getPackageName()` / `getProcessName()` 过滤作用域
- Hook 逻辑包在 `try/catch (Throwable)` 中，失败时 `log("...", t)`
- 保持改动最小；不修改 `libxposed-compat` 除非注解层有需求
- 构建命令：`module/gradlew.bat assembleRelease`（Windows）
