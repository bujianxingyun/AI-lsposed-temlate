---
description: 公众网表单验证提示规范。编写或修改表单、校验逻辑、el-form 时遵循。
globs: src/**/*.vue,src/**/*.js
alwaysApply: false
---

# 表单验证提示

参考：`design/公众网全局提示规范.md` 第一章

## 交互效果

- 校验失败：控件边框变红（`#F44336`），focus 时带 `box-shadow: 0 0 0 3px rgba(244, 67, 54, 0.1)`
- 控件下方显示红色错误文案（12px，`line-height: 1.5`，`margin-top: 4px`）
- 用户输入/选择后自动清除该字段错误状态

## Vue 模板结构

```vue
<div class="form-item" :class="{ 'form-item-error': errors.region }">
  <label class="form-label required">项目所属区域</label>
  <el-select v-model="form.region" @change="clearFieldError('region')">
    <el-option label="请选择" value="" />
  </el-select>
  <div class="form-error-message" :class="{ show: errors.region }">
    请选择项目所属区域
  </div>
</div>
```

必填项 label 使用 `.form-label.required`，通过 `::before` 显示红色 `*`。

## 校验逻辑

```javascript
// 显示错误：errors[field] = true，并 focus 对应控件
showFieldError(field) {
  this.$set(this.errors, field, true)
  this.$refs[field]?.$el?.querySelector('input, textarea, select')?.focus()
}

clearFieldError(field) {
  this.$set(this.errors, field, false)
}
```

提交前逐字段校验，**第一个错误字段**调用 `showFieldError` 并 `return`，不要用 Toast 替代字段级提示。

## Element UI 表单

使用 `el-form` 时：

- 关闭 Element 默认 error 样式冲突：表单项外包 `.form-item`，用规范 CSS 覆盖 `.form-item-error` 下 `.el-input__inner` / `.el-textarea__inner` 边框色
- `el-form-item` 的 `:rules` 可保留，但错误展示须符合规范视觉（字段下方红字，非右侧 icon 弹层）
- 选择框、输入框、文本域三种控件错误态样式保持一致

## 错误文案

- 输入框：`请输入{字段名}`
- 选择框：`请选择{字段名}`
- 文案与 label 语义一致，简洁明确
