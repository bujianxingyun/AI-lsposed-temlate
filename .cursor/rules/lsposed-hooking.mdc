---
description: libxposed API 101 Hook 编写规范（LSPosed）
globs: module/app/src/**/*.java
alwaysApply: false
---

# LSPosed Hook 规范

本模板使用 **libxposed API 101**。入口类继承 `XposedModule`，**无构造函数**。

## 生命周期

| 回调 | 用途 |
|------|------|
| `onModuleLoaded` | 模块加载；可读取框架信息 |
| `onPackageLoaded` | 包进入进程（勿在此 Hook） |
| `onPackageReady` | **应用 Hook 入口** |
| `onSystemServerStarting` | system_server Hook；scope 需含 `system` |

同一进程可能加载多个包，**必须**先判断 `getPackageName()`；进程名用 `onModuleLoaded` 的 `ModuleLoadedParam.getProcessName()` 或 `PackageLoadedParam.getApplicationInfo().processName`（`PackageReadyParam` 无 `getProcessName()`）。

## Hook 模式

```java
@Override
public void onPackageReady(@NonNull PackageReadyParam param) {
    if (!param.getPackageName().equals("com.target.app")) return;
    try {
        Method method = param.getClassLoader()
                .loadClass("com.target.Foo")
                .getDeclaredMethod("bar", String.class);

        hook(method)
                .setPriority(PRIORITY_DEFAULT)
                .setExceptionMode(ExceptionMode.PROTECTIVE)
                .intercept(chain -> chain.proceed());
    } catch (Throwable t) {
        log(Log.ERROR, TAG, "Hook failed", t);
    }
}
```

## 禁止事项

- ❌ 使用 `XposedHelpers`、`IXposedHookLoadPackage` 等 Classic API
- ❌ 在 `onPackageLoaded` 中 Hook（应等 `onPackageReady`）
- ❌ 使用 `XResources.setReplacement`（libxposed 不支持）
- ❌ 在 Hook 回调中抛出未捕获异常

## 调用原方法

```java
getInvoker(method).setType(Invoker.Type.ORIGIN).invoke(chain.getThisObject(), args);
```

Inline 优化导致 Hook 失效时，可调用 `deoptimize(executable)`。
