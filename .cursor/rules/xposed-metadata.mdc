---
description: META-INF/xposed 模块元数据与作用域配置
globs: module/app/src/main/resources/META-INF/xposed/**
alwaysApply: false
---

# Xposed 模块元数据

配置文件位于 `module/app/src/main/resources/META-INF/xposed/`。

## java_init.list

每行一个入口类 FQCN，必须与 `MainModule` 的实际包名一致：

```
com.example.module.MainModule
```

## scope.list

每行一个目标包名；Hook system_server 时使用 `system`（非 `android` 或 `com.android.providers.settings`）。

```
system
com.target.app
```

`module.prop` 中 `staticScope=true` 表示作用域在编译期固定，运行时不可在 LSPosed 中修改。

## module.prop

| 键 | 本模板值 | 说明 |
|----|----------|------|
| `minApiVersion` | 100 | 最低 libxposed API |
| `targetApiVersion` | 100 | 目标 API；升级 API 101 需同步改 Hook 写法 |
| `staticScope` | true | 静态作用域 |

模块名称与描述通过 `AndroidManifest.xml` 的 `android:label` / `android:description` 设置，**不要**在 `module.prop` 里写 name/description。

## 修改 scope 后

在 LSPosed 管理器中重新勾选模块作用域并重启目标进程。
