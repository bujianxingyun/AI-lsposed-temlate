---
description: META-INF/xposed 模块元数据与作用域配置
globs: module/app/src/main/resources/META-INF/xposed/**
alwaysApply: false
---

# Xposed 模块元数据

配置文件位于 `module/app/src/main/resources/META-INF/xposed/`。

## java_init.list

每行一个入口类 FQCN，必须与 `MainModule` 的实际包名一致：

```
com.example.module.MainModule
```

## scope.list

每行一个目标包名；Hook system_server 时使用 `system`。

本模板默认为空（仅注释示例），请在定制时添加目标包名。`staticScope=false` 时用户可在 LSPosed 中扩展作用域。

## module.prop

| 键 | 本模板值 | 说明 |
|----|----------|------|
| `minApiVersion` | 101 | 最低 libxposed API |
| `targetApiVersion` | 101 | 目标 API |
| `staticScope` | false | false 允许 LSPosed 中调整作用域 |

模块名称与描述通过 `AndroidManifest.xml` 的 `android:label` / `android:description` 设置。

## 修改 scope 后

在 LSPosed 管理器中重新勾选模块作用域并重启目标进程。
