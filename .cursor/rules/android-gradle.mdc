---
description: Android Gradle 构建与依赖约定
globs: module/**/*.gradle
alwaysApply: false
---

# Gradle 构建约定

## 依赖

libxposed 必须为 `compileOnly`：

```gradle
compileOnly 'io.github.libxposed:api:101.0.1'
```

❌ 不要用 `implementation` 引入 libxposed API。

## SDK 与 Java

- `minSdk 26`，`compileSdk` / `targetSdk` 默认 35
- Java **21**（`sourceCompatibility` / `targetCompatibility`）
- `debug`：`minifyEnabled false`（日常开发）
- `release`：`minifyEnabled true` + `proguard-rules.pro`

## 仓库

Maven 使用阿里云镜像（`settings.gradle`），libxposed 从 Maven Central 解析，**无需** `mavenLocal`。

## 版本

- AGP **8.7.3**，Gradle **8.9**（Wrapper 使用阿里云 `mirrors.aliyun.com/gradle/distributions` 镜像）
- 升级 libxposed 时同步修改 `module.prop` 与 Hook 代码

## 常见修改

| 需求 | 修改位置 |
|------|----------|
| 改包名 / applicationId | `app/build.gradle` + Java 目录 + `java_init.list` |
| 改 Hook 目标 | `MainModule.TARGET_PACKAGE` + `scope.list` |
| 固定作用域不可改 | `module.prop` 设 `staticScope=true` |
